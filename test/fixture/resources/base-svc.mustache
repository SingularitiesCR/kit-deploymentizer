apiVersion: v1
kind: Service
metadata:
  name: {{{svc.name}}}
  labels:
  {{#svc.labels}}
    {{{name}}}: {{{value}}}
  {{/svc.labels}}
  {{#deployment.fastRollback}}
    {{#deployment.id}}
    id: {{{deployment.id}}}
    {{/deployment.id}}
  {{/deployment.fastRollback}}
